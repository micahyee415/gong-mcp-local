{
  "name": "gong-mcp-admin",
  "version": "1.0.0",
  "description": "Uncapped local Gong MCP server for admin use — no blacklist, no date defaults, raised per-tool caps.",
  "type": "module",
  "bin": {
    "gong-mcp-admin": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "smoke": "node __tests__/smoke.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "dotenv": "^17.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
